import React from 'react';
import MenuItem from '@material-ui/core/MenuItem';

const NoOptions = () => (
  <MenuItem
    component="div"
    style={{
      fontWeight: 600,
    }}
  >
    No options
  </MenuItem>
);

export default NoOptions;
